import { colors } from "@/constants/colors";
import { Text, View } from "react-native";

export default function Index() {
  return (
    <View style={{ backgroundColor: colors.crest, minHeight: "100%" }}>
      <Text>Edit app/index.tsx to edit this screen.</Text>
    </View>
  );
}
